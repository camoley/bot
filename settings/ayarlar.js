require("dotenv").config();

module.exports = {
    TOKEN: process.env.TOKEN,  // bot token
    PREFIX: process.env.PREFIX || "-", // bot prefix
    SAHİP: process.env.SAHİP || "278590588390211595", //sahip id
}